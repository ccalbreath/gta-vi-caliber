name: Engine

on:
  push:
    branches: [main]
    paths:
      - "engine/**"
      - ".gitmodules"
      - ".github/workflows/engine.yml"
  pull_request:
    paths:
      - "engine/**"
      - ".gitmodules"
      - ".github/workflows/engine.yml"

env:
  GODOT_VERSION: "4.6.3"
  SCONS_CACHE: ${{ github.workspace }}/.scons-cache

jobs:
  build:
    name: Build & test (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux
            godot-zip: linux.x86_64
            godot-bin: Godot_v4.6.3-stable_linux.x86_64
          - os: windows-latest
            platform: windows
            godot-zip: win64.exe
            godot-bin: Godot_v4.6.3-stable_win64.exe
          - os: macos-latest
            platform: macos
            godot-zip: macos.universal
            godot-bin: Godot.app/Contents/MacOS/Godot
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          submodules: recursive

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install SCons
        run: pip install scons

      # godot-cpp's SConstruct enables CacheDir() when SCONS_CACHE is set,
      # so the ~10 min bindings compile only happens on a cold cache.
      - name: Cache SCons build
        uses: actions/cache@v4
        with:
          path: ${{ env.SCONS_CACHE }}
          key: scons-${{ matrix.platform }}-${{ hashFiles('.gitmodules', 'engine/SConstruct') }}-${{ hashFiles('engine/src/**') }}
          restore-keys: |
            scons-${{ matrix.platform }}-${{ hashFiles('.gitmodules', 'engine/SConstruct') }}-
            scons-${{ matrix.platform }}-

      - name: Build GDExtension (template_debug)
        run: scons -C engine platform=${{ matrix.platform }} target=template_debug

      # Also build the release target so the path that ships in release.yml is
      # validated on every PR — not discovered broken at tag time.
      - name: Build GDExtension (template_release)
        run: scons -C engine platform=${{ matrix.platform }} target=template_release

      - name: Build & run C++ unit tests
        run: |
          scons -C engine tests
          if [ -x engine/bin/test_worldcore ]; then
            ./engine/bin/test_worldcore
          else
            ./engine/bin/test_worldcore.exe
          fi

      - name: Cache Godot
        id: cache-godot
        uses: actions/cache@v4
        with:
          path: ~/godot-bin
          key: godot-${{ env.GODOT_VERSION }}-${{ matrix.platform }}

      - name: Download Godot
        if: steps.cache-godot.outputs.cache-hit != 'true'
        run: |
          mkdir -p ~/godot-bin
          curl -sSL -o /tmp/godot.zip \
            "https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_${{ matrix.godot-zip }}.zip"
          unzip -q /tmp/godot.zip -d ~/godot-bin

      # Without the manifest the engine never loads the freshly built lib and
      # every "with native module" step below silently tests the GDScript
      # fallback instead.
      - name: Install native manifest
        run: |
          mkdir -p game/bin
          cp engine/worldcore.gdextension.example game/bin/worldcore.gdextension

      - name: Headless import with native module
        run: |
          GODOT=~/godot-bin/"${{ matrix.godot-bin }}"
          chmod +x "$GODOT" || true
          # First import after a native build can crash *at exit* while the
          # editor generates extension docs (import completes; upstream bug,
          # see engine/README.md). Tolerate it once, then require clean runs.
          "$GODOT" --headless --path game --import || echo "first import exited $? (known doc-gen crash, retrying)"
          "$GODOT" --headless --path game --import

      - name: Smoke test with native module
        run: ~/godot-bin/"${{ matrix.godot-bin }}" --headless --path game --script res://tests/smoke_test.gd

      - name: Unit tests with native module (asserts WorldCore loads)
        run: |
          GODOT=~/godot-bin/"${{ matrix.godot-bin }}"
          # gdUnit4-discovered suites, run with the freshly built native lib present.
          "$GODOT" --headless --path game --script res://tests/run_tests.gd
          # The WorldCore-loaded assertion lives in test_worldcore.gd, a legacy
          # RefCounted suite that gdUnit4's discoverer skips — so it only runs via
          # run_legacy_tests.gd. The native lib was just built, so it must NOT skip.
          out=$("$GODOT" --headless --path game --script res://tests/run_legacy_tests.gd)
          echo "$out"
          echo "$out" | grep -q "WorldCore .* loaded from native module"

      - name: Native module integration probes (benchmarks, crowd capacity)
        run: |
          # These standalone probes exercise the native modules end to end
          # (SpatialHash/CrowdSteering/TrafficModel) and the perf benchmarks, and
          # quit(1) on failure. The native lib was just built, so they run the
          # real native path rather than the absent-module skip.
          GODOT=~/godot-bin/"${{ matrix.godot-bin }}"
          for probe in native_bench_probe crowd_capacity_probe; do
            echo "== ${probe} =="
            "$GODOT" --headless --path game --script "res://tests/${probe}.gd"
          done
