# gdtoolkit lint configuration (gdlint reads this from repo root)
# Vendored addons are excluded; line length relaxed to 120.

disable: []

excluded_directories: !!set
  .godot: null
  addons: null

max-line-length: 120
max-file-lines: 1000
max-public-methods: 25
